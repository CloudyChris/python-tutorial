# The basic syntax of python is: expressions are separated by newlines and scopes are differentiated by identation (standard is 4 spaces or 1 tab (of ideally 4 spaces))
# Example:

my_name = "Richard"
my_nickname = "Dick"
my_age = 2503

if my_age > 30:
    my_nickname = "Old " + my_nickname
